# 受保护文件——改动需 owner 审阅后打 `owner-approved` 标签才能合并。
#
# 实际的合并阻断由 .github/workflows/protected-paths-guard.yml 做（required check
# `guard`：碰下面这些路径 + 没 `owner-approved` 标签 → 失败）。本 CODEOWNERS 文件的
# 作用是让 GitHub 在这类 PR 上自动向 @HRLoveFun 请求 review，是可见的提醒——solo 仓库
# 里 GitHub 原生的 "Require review from Code Owners" 挡不住 auto-merge（见
# [ADR-protected-paths-ci-guard]）。不碰这些路径的 PR 流水线不变。
#
# 依据见 PROJECT/DECISIONS.md 的 [ADR-protected-files-approval-gate] / [ADR-protected-paths-ci-guard]。

# 会话宪法
/CLAUDE.md                @HRLoveFun

# 数据结构 / 术语 / 枚举 / spec 形状的权威
/schema/                  @HRLoveFun

# CI 与仓库自动化
/.github/                 @HRLoveFun

# 命令的唯一权威
/Makefile                 @HRLoveFun
